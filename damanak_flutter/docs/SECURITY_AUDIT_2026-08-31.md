# تدقيق ضمانك الأمني — 2026-08-31

## النطاق والنتيجة

- المراجعة: تدقيق ساكن شامل لمجلد `damanak_flutter/` عند الالتزام `eba5c66d975b4c99901d5fe712a6703b1004700e`، ثم مراجعتان مستقلتان لفروق الإصلاح.
- معرّف الفحص: `8e88a5dc-f06f-40ea-a916-b89e0db235b5`.
- النتيجة الأصلية: 5 نتائج قابلة للتقرير؛ 3 متوسطة و2 منخفضة، ولا توجد نتيجة عالية أو حرجة.
- شمل الفحص: الدخول وRLS وعزل المتاجر، تحقق Apple وGoogle، التجارب، الدعوات والمقاعد، الروابط العامة والمرفقات، CSV، الذكاء الاصطناعي، API وWebhooks.
- لا يثبت التدقيق قبول شراء حقيقي أو سلوك جهاز iPhone/Android؛ تبقى هذه اختبارات جهاز منفصلة.

## الحالة بعد الإصلاح

| النتيجة | الشدة | الحالة | الدليل |
| --- | --- | --- | --- |
| مراجع ضمان أو مطالبة عابرة للمتاجر | متوسطة | مغلقة | migrations `20260831140000` و`20260831150000` مطبقتان؛ أضيفت القيود المركبة للضمان ومطالباته. رابط العميل يفلتر المطالبة بـ`warranty_id` و`store_id`. نجح اختبار حي بمتجرين ورفض المراجع الخمسة وانحراف الأب والمطالبة العابرة ثم نفذ `ROLLBACK` بلا بيانات باقية. |
| حقن صيغ برامج الجداول عبر CSV | متوسطة | مغلقة في المصدر | كل الحقول النصية غير الموثوقة تُعامل كنص، والأعمدة الرقمية الثلاثة فقط تُصدّر كأرقام محدودة. يلزم وصولها للمستخدم في البناء التالي. |
| تحليل جسم المطالبة قبل الحد | منخفضة | مغلقة ومنشورة | `warranty-card` v14 يعد البايتات الفعلية قبل `formData()`، ويحد JSON إصدار الرابط بـ64 KiB بعد تحقق المستخدم. |
| تكرار التجربة بعميل معدل | متوسطة | مفتوحة خارجياً | الحارس الحالي يمنع التكرار العرضي للحساب والتثبيت الرسمي، لكن UUID من العميل ليس إثبات جهاز. الإغلاق الكامل يحتاج App Attest/DeviceCheck وPlay Integrity وقراراً لمسار الويب. |
| روابط الضمان طويلة وغير قابلة للإبطال المنفرد | منخفضة | تحتاج قرار منتج | يلزم تحديد مهلة الرابط، وسياسة روابط v1 الحالية، وهل يكفي تدوير كل روابط الضمان أم يلزم إبطال رابط واحد. لم تُكسر روابط العملاء الحالية بافتراض صامت. |

## ضوابط مؤكدة

- لا يمنح التطبيق خطة مدفوعة قبل تحقق الخادم من إيصال Apple أو Google.
- ربط الإيصال بالمتجر ذري، وإعادة استخدام الإيصال لمتجر آخر مرفوضة.
- الدعوات محدودة بالدور والمقاعد والاستخدام والانتهاء، والرموز الفعلية تقبل حصراً أطوال hexadecimal `10/16/32` بعد `DMN-`.
- مسارات الذكاء الاصطناعي معزولة بالمتجر والحصة والدور، ولا تمنح قرار مطالبة تلقائياً.
- صفحة الضمان العامة تقلل بيانات العميل وتستخدم HMAC ورؤوس `no-store` و`no-referrer` وCSP.

## متابعة دورة الاشتراك في مصدر 4.5.0+24

- راجعت المتابعة كامل مسار الكتالوج والشراء والاستعادة والتغيير والإلغاء والمصالحة وحذف الحساب على Flutter وEdge Functions وSQL. لا تغيّر هذه المتابعة نتيجة التدقيق الأصلية ذات النتائج الخمس؛ هي تحصين وظيفي وأمني إضافي بعده.
- Apple Build `24` يربط المعاملة بـUUID المتجر عبر `appAccountToken`، ويرفض الحدث المتأخر غير المطابق. يبقى توافق Build `23` القديم مقيداً بملكية الحساب ومتجر واحد أو إيصال مرتبط سابقاً.
- Google يفحص نتيجة `BillingClient` الحقيقية، ولا يفسر الفشل كغياب مشتريات. توكن الاستبدال يجب أن يطابق الحساب والمتجر صراحةً؛ legacy غير المربوط لا يدخل `launchBillingFlow` أبداً.
- الاستعادة تختار المتجر المفتوح من حساب متعدد المتاجر، وتفضل الربط الدقيق، ولا ترسل للخادم أكثر من مرشح legacy واحد عند غياب التطابق. النتائج المعلقة والحسابات والمتاجر الأخرى لا تتحول إلى نجاح استعادة.
- migration `20260831180000_damanak_google_lineage_predecessor_cas.sql` يفرض predecessor/current-receipt بنمط CAS ويرفض sibling أو replay. حذف المالك في `20260831170000` يثبت الخلف النشط قبل إلغاء الاشتراك.
- `verify-store-purchase` v23 و`refresh-store-entitlements` v7 نشطتان، وإقرار Google يتم بعد تطبيق الاستحقاق مع إعادة محاولة مجدولة. آخر تشغيل `damanak-entitlement-refresh` عند `2026-08-31 20:35 UTC` نجح وأعاد HTTP `200`.
- لا يوجد Google RTDN حالياً؛ شراء خارجي جديد لا يُكتشف إلا عند فتح التطبيق. هذه فجوة تشغيل موثقة لا مبرر للادعاء بإغلاقها، ولا تعني إمكان تجاوز عزل الإيصال عند وصوله للخادم.

## متابعة المتجر بلا تجربة في مصدر 4.5.0+25

- لا تغيّر هذه المتابعة عدد نتائج التدقيق أو حالة نتيجة إثبات الجهاز. UUID التثبيت وحجز الحساب/الجهاز يحدان التكرار العرضي، لكنهما ليسا App Attest/DeviceCheck أو Play Integrity.
- migration `20260901010000_damanak_paid_store_without_repeat_trial.sql` تضيف RPC جديدة لـBuild `25` وتبقي `create_store_with_trial` القديمة بلا تغيير لتوافق Build `24`. الحساب غير المؤهل يحصل على متجر idempotent مع placeholder غير قابل للاستعمال، بلا استحقاق أو فرع أو حصة.
- إنشاء الفرع الرئيسي مؤجل حتى يصبح الاشتراك قابلاً للاستعمال، ثم ينشئ trigger الاشتراك `MAIN` مرة واحدة. يمنع ذلك فتح واجهة تشغيل ناقصة أو تجاوز حراس الكتابة أثناء bootstrap.
- حالة أول دفع فقط تُحجب عنها الدعوات والمطالبات الجديدة وتحديثها وقراءة الضمان الموثقة اللازمة لإصدار رابط جديد. الاشتراك التاريخي المنتهي لا يطابق العلامة الدقيقة، لذلك لم تُكسر استمرارية الضمانات والمطالبات والروابط القديمة بافتراض صامت.
- migration `20260901011000_damanak_paid_store_acl.sql` تسحب تنفيذ RPC الجديدة ودوال الحراسة من `service_role` وتبقي نقطة الدخول الجديدة لـ`authenticated` فقط. الحراس الداخلية تعمل بصلاحية مالكها داخل triggers وRLS.
- نجح `store_creation_without_repeat_trial_live.sql` على الإنتاج داخل `BEGIN/ROLLBACK`، وأثبت توافق Build `24`، idempotency، غياب الحصة والفرع والاستحقاق، رفض عمليات التشغيل قبل الدفع، وإنشاء `MAIN` مرة واحدة بعد تفعيل صالح.
- لا يثبت هذا نجاح شراء أو استعادة حقيقيين، ولا يغلق مسار عميل معدل يستطيع تزوير UUID. الإغلاق الكامل لنتيجة التجربة يحتاج خدمات إثبات المنصة وقراراً صريحاً لمسار الويب.

## متابعة استعادة Apple الحية في مصدر 4.5.0+26

- أثبتت سجلات TestFlight شراء Apple ناجحاً ثم حذف حساب ضمانك بعد نحو 8 ثوانٍ، مع بقاء الاشتراك لدى Apple. جلب App Store Server API السلسلة القديمة بنجاح عند الاستعادة إلى حساب جديد، لكن `SignedDataVerifier` المتصل رفض JWS قبل أي كتابة؛ بقي الحساب الجديد بلا entitlement.
- `verify-store-purchase` v26 تبقي الفحص المتصل أولاً. فقط المهلة والحالات `1/2/6` تعاد عبر وضع Apple الرسمي الذي يتحقق من التوقيع وسلسلة `x5c` والجذور المثبتة وصلاحيتها عند `signedDate` ومعرّف التطبيق والبيئة. الحالة `FAILURE=7`، التي قد تمثل OCSP غير `good`، وهوية التطبيق والبيئة وطول السلسلة تبقى fail-closed.
- لا يكفي نجاح الفحص الاحتياطي: تبقى المطابقة الحرفية مع App Store Server API لـ`bundleId/transactionId/originalTransactionId/productId/environment/appAccountToken`، ثم حواجز حذف الهوية القديمة والمتجر الوحيد وغياب entitlement أو ربط منافس، ثم القيد الذري في قاعدة البيانات. لم تجد المراجعة العدائية المستقلة مسار استيلاء أو replay جديداً في الرقعة بعد استبعاد الحالة `7`.
- لا تغير هذه المتابعة النتيجتين المفتوحتين لإثبات الجهاز وروابط الضمان، ولا تثبت نجاح الاستعادة بعد النشر حتى يعيد المستخدم المحاولة من TestFlight.

## اختبارات إعادة التشغيل

```powershell
flutter analyze
flutter test
npx --yes deno test --allow-env --allow-net supabase/functions
npx --yes deno fmt --check supabase/functions
npx supabase db query --linked --file supabase/tests/store_creation_without_repeat_trial_live.sql --output json
npx supabase db query --linked --file supabase/tests/warranty_tenant_integrity_live.sql --output json
flutter build web --release
flutter build apk --debug
```

اختبار SQL الحي محاط دائماً بـ`BEGIN/ROLLBACK`، ويعطل `USER` triggers داخل المعاملة فقط مع إبقاء قيود FK الداخلية فعالة.

## تحقق الإغلاق

- `flutter analyze`: ناجح بلا ملاحظات.
- Flutter: `160/160` اختباراً ناجحاً.
- Deno: `56/56` اختباراً ناجحاً.
- نجح `git diff --check` وفحص تنسيق ملفات الدوال.
- نجح بناء web release وAndroid debug. APK Build `25` المحلي حجمه `180,814,040` بايت وبصمته `C3070BABAA8F452C23B6BBF5D71881C4FD1BF8BDCFAC44A64B39CA723EF1B775`؛ لا يمثل ذلك قبول شراء أو قارئ شاشة أو جهاز فعلي.
- نجحت اختبارات `store_creation_without_repeat_trial_live.sql` و`subscription_lifecycle_live.sql` و`account_deletion_successor_live.sql` و`warranty_tenant_integrity_live.sql` على الإنتاج داخل `BEGIN/ROLLBACK`.
- راجع وكيلان مستقلان رقع Edge/SQL وFlutter بعد migration `20260831180000`. أُصلحت ملاحظاتهما حول نتيجة BillingClient، legacy replacement، واختيار المتجر المتعدد، ثم لم تبق نتيجة إضافية قابلة للتقرير ضمن النطاق.
