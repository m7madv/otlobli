# إعداد فوترة ضمانك عبر المتاجر

إصدار المصدر الحالي: `4.5.0+29` — معرّف التطبيق: `com.damanak.damanak`

Build `23` هو حزمة App Review وGoogle Alpha، وBuildات `24` حتى `28` موجودة في iPhone TestFlight فقط. مصدر Build `29` يعيد تنظيم Flutter حول آلة حالة وسياسة انتقال مركزيتين ويبقي فحص ملكية Apple ومنع الخفض من داخل التطبيق؛ لم يُرفع بعد وقت كتابة هذه الحالة. Build `26` ينظم الاستعادة بعد حذف حساب ضمانك وحالات الفوترة العالقة، وإصلاح `verify-store-purchase` v28 خادمي ويستفيد منه Build `26` فوراً. لا تنسب تغييرات Build `29` إلى بناء أقدم، ولا تضف بناءً إلى App Review أو Google من دون قرار منفصل.

## الحالة الخارجية

- Apple App ID: `6804792494`. submission `69104e50-7a9a-4102-a717-ca341d442897` يضم الإصدار `4.5.0` ومجموعة الاشتراك والمنتجات الستة، أي 8 عناصر، وحالته `Waiting for Review` على Build `23`.
- iPhone TestFlight Build `4.5.0 (24)` رُفع في run [33443358450](https://github.com/m7madv/otlobli/actions/runs/33443358450)، وأنهت Apple معالجته. حالته `Ready to Submit` ومربوط بمجموعة `Damanak Internal` الداخلية ذات المختبر الواحد؛ لم يُضف إلى App Review. IPA حجمها `23,923,863` وبصمتها `EA55D5F1192886EE9019F0048E3476C1D5416E047B4F1F1823D234FC56BCF768`.
- رفع run [33449769807](https://github.com/m7madv/otlobli/actions/runs/33449769807) Build `4.5.0 (25)` إلى Apple بنجاح. أنهت Apple المعالجة؛ البناء `VALID` ومعرّفه `8e864062-28e0-4250-abd2-dac25f8df1b9` وداخل `Damanak Internal` ذات المختبر الواحد. الـIPA حجمها `23,930,380` بايت وبصمتها `D2DAF9EF0A97B3C8A6328C32D1B71276352DEAA29A97E144653978983F13A14F`. لم يُضف إلى App Review.
- رفع run [33458183412](https://github.com/m7madv/otlobli/actions/runs/33458183412) الالتزام `4a5e96bdd1eefd3f27ff89fccf47fe18012ef4f7` بوصفه iPhone Build `4.5.0 (26)`. نجح التحقق والرفع، ثم أكد فحص القراءة [33459040483](https://github.com/m7madv/otlobli/actions/runs/33459040483) أن البناء `VALID` بالمعرّف/Delivery UUID `10a1e004-c6c1-4b56-9cff-618406c3881b` وداخل `Damanak Internal` ذات المختبر الواحد. الـIPA حجمها `23,942,352` بايت وبصمتها `FE66B7F77FB1892FE8F3D55F0AC85AB6B0A7DF783286C77C2EDA735A82C32A37`. لم يُضف إلى App Review.
- رفع run [33481729437](https://github.com/m7madv/otlobli/actions/runs/33481729437) الالتزام `e073cd5f7fad489f094337a142c46786eda2d57f` بوصفه iPhone Build `4.5.0 (27)`. نجح التوقيع والتحقق والرفع، ثم أكد فحص القراءة [33482932576](https://github.com/m7madv/otlobli/actions/runs/33482932576) أن البناء `VALID` بالمعرّف/Delivery UUID `5b04b5da-22e5-4cc9-affc-691e3b7e840f` وداخل `Damanak Internal` ذات المختبر الواحد، مع بقاء إصدار App Store في `Waiting for Review` ومن دون mutation. الـIPA حجمها `23,943,985` بايت وبصمتها `808712BDD6E46942C9CE837B01FC329ED1CE25A3FC47E95F577BEC03790E2547`. لم يُضف إلى App Review.
- نجح فحص Build `4.5.0 (28)` الموقّع بلا رفع في run [33520286056](https://github.com/m7madv/otlobli/actions/runs/33520286056)، ثم رفعه run [33520953615](https://github.com/m7madv/otlobli/actions/runs/33520953615) من الالتزام `baaa3e151036d280d8c2ca17f5b069f3aa9e44e8`. أكد inspect [33522527864](https://github.com/m7madv/otlobli/actions/runs/33522527864) أنه `VALID` بالمعرّف/Delivery UUID `a9038423-e4fc-4499-8c04-bba4c0f1d317` وداخل `Damanak Internal` ذات المختبر الواحد، مع بقاء App Store `Waiting for Review` و`mutationRequested=false`. الـIPA حجمها `23,963,467` بايت وبصمتها `230304290CE79C5E8F47A1D511DC8C845846C88BF529F7D167CA87B07BA65561`. لم يُثبت على جهاز حقيقي ولم يُضف إلى App Review.
- مصدر Build `4.5.0 (29)` اجتاز محلياً `flutter analyze` و`211/211` اختبار Flutter و`69/69` اختبار اشتراك مركز و`77/77` اختبار Deno وبناء web release وAndroid debug. رفع iPhone الموقّع والتحقق من App Store Connect لم يُنفذا بعد، ولا يُنسبان إلى Build `28`.
- أضيف رابط EULA القياسي إلى وصف App Store، وأُكد للمراجعة أن أسعار «توسع» المقصودة تأتي من App Store. لا توجد أسعار بديلة داخل التطبيق.
- Google Build `23` موجود على مسار Alpha رقم `4699097266255892286` وقيد المراجعة. قائمة `All Apps - Closed Testers` دائمة وتضم بالضبط 12 مختبراً؛ لا تُنشئ قائمة أخرى ولا تعدّلها.
- رابط الانضمام هو `https://play.google.com/apps/testing/com.damanak.damanak`. لا يبدأ عداد 14 يوماً إلا بعد قبول Google وانضمام المختبرين فعلياً.
- ثبت Build `26` وشراء Apple حقيقي على iPhone من TestFlight. حذف المستخدم حساب ضمانك بعد نجاح الشراء مباشرةً، فبقي الاشتراك لدى Apple وصارت استعادته orphan. بين `08:36:15` و`08:38:40` بتوقيت الرياض وصلت سبع محاولات v27 للمتجر نفسه؛ أعاد الفاحصان الرسميان الحالة `1` ثم رفض فاحص التوافق بسبب عدم توافق X509 مع Deno `2.1.4`، بلا كتابة أو رد `200`. بعد نشر v28 نجحت محاولة المستخدم عند `09:22:26`: ظهر نجاح توافق Apple وأعاد التحقق HTTP `200`، وربط باقة «توسع» الشهرية فعلياً بالمتجر النهائي بلا شراء ثانٍ. لم يُثبت شراء أو استعادة Android.

## عقد المنتج

- اشتراك ضمانك منفصل تماماً عن مبيعات صاحب المتجر لعملائه داخل نقطة البيع.
- الباقات هي بداية/نمو/توسع بحصص `100/600/3000` ضمان شهرياً وحدود فريق `2/5/15`.
- السعر والعملة والنص المحلي تأتي من App Store أو Google Play فقط. عند غياب الكتالوج لا يعرض التطبيق سعراً محلياً مصطنعاً.
- التجربة مرة واحدة للحساب والتثبيت. عند عدم الأهلية يُنشأ المتجر بلا فرع أو حصة أو ميزات مجانية، ولا يُفتح التشغيل إلا بعد اشتراك مدفوع متحقق أو استعادة استحقاق فعّال.
- لا تُفعّل الخطة عند الدفع المعلّق، ولا عند نجاح نافذة الدفع وحدها؛ الاستحقاق يحتاج تحقق الخادم أولاً.
- لا يُسمح باشتراكين متزامنين من مزودين مختلفين للمتجر نفسه. الإدارة والاستعادة تستخدمان المزود المحفوظ في الاشتراك، لا منصة الجهاز افتراضياً.
- منتجات Apple داخل مجموعة اشتراك واحدة، وGoogle يستخدم استبدال الاشتراك الحالي؛ اختيار باقة أعلى هو ترقية داخل الاشتراك نفسه لا اشتراكاً متوازياً. الباقة الأدنى تبقى ظاهرة للمقارنة فقط وغير قابلة للشراء أثناء سريان باقة أعلى، ونفس الخطة بدورة أخرى «تغيير فوترة».
- الحصة الشهرية سقف وليست رصيداً قابلاً للجمع. عند الترقية يحل سقف الخطة الجديدة محل القديم مع بقاء ضمانات الشهر محسوبة: استخدام 30 من بداية ثم نمو يعني `600 - 30 = 570` متبقياً. تغيير دورة الفوترة لا يعيد الحصة.
- بعد تحديث حالة الاشتراك من الخادم يعيد التطبيق تصنيف الانتقال؛ إذا كانت الخطة والدورة حاليتين أو كانت الخطة المستهدفة أدنى، لا يفتح نافذة المتجر. تعيد خدمة الشراء الحراسة مستقلاً حتى لو استدعاها مسار واجهة قديم.
- لا يمكن منع الخفض من صفحة إدارة اشتراكات Apple نفسها ما دامت الباقات الأدنى متاحة في المجموعة. مستويات Apple المقصودة: توسع `1`، نمو `2`، بداية `3`، مع الشهري والسنوي في المستوى نفسه. إذا أرسل Apple أو Google حالة صحيحة نفذها المستخدم خارج ضمانك، يجب على التحقق الخادمي مصالحتها؛ لا يجوز تحصيل سعر باقة أدنى مع إبقاء مزايا أعلى.

## منتجات Apple

مجموعة `Damanak Plans` والمنتجات الستة موجودة في App Store Connect:

| الخطة | شهري | سنوي |
|---|---|---|
| بداية | `com.damanak.subscription.starter.monthly` | `com.damanak.subscription.starter.yearly` |
| نمو | `com.damanak.subscription.growth.monthly` | `com.damanak.subscription.growth.yearly` |
| توسع | `com.damanak.subscription.scale.monthly` | `com.damanak.subscription.scale.yearly` |

التوفر في دول الخليج الست. الأسعار المعروضة للمستخدم تؤخذ من StoreKit؛ لا تغيّر المعرّفات ولا تضف fallback محلياً. Build `24` وما بعده يرسل UUID المتجر في `applicationUserName` ليحفظه StoreKit 2 بوصفه `appAccountToken`. الحدث المتأخر أو المصطف لا يُربط بالمتجر إلا إذا طابق هذا الرمز. Build `26` يستطيع استعادة إيصال Build `23` ذي UUID الحساب المحذوف فقط عند إجراء المستخدم الصريح وبعد تحقق JWS وسلسلة Apple وحذف الهوية القديمة وكون الهدف متجراً وحيداً بلا entitlement؛ ثم يحدث Apple token إلى UUID المتجر بعد التطبيق الذري.

قبل كل شراء Apple، يستعلم Build `29` محلياً عن `Transaction.currentEntitlements` ويرجع فقط `productId` و`originalTransactionId` و`appAccountToken` إن وجد؛ لا يعيد JWS ولا يربط اشتراكاً. بلا اشتراك خادمي، وجود أي entitlement حالي لضمانك يمنع الشراء ويطلب استعادة صريحة. مع اشتراك خادمي، يجب أن يملك Apple ID الحالي سلسلة `originalTransactionId` نفسها والمنتج الحالي نفسه قبل فتح StoreKit. غياب الاستحقاق أو اختلاف الحساب/السلسلة/المنتج أو فشل الاستعلام يمنع الدفع، ولا يوجد recovery صامت.

## منتجات Google

المنتجات الثلاثة هي:

- `com.damanak.subscription.starter`
- `com.damanak.subscription.growth`
- `com.damanak.subscription.scale`

لكل منتج خطتان أساسيتان بالمعرّفين الدقيقين `monthly` و`yearly`. التطبيق يختار العرض الأساسي المباشر الذي لا يحمل `offerId`، ويعرض السعر الذي يعيده Google Play.

Build `24` وما بعده يرسل `obfuscatedAccountId` لحساب ضمانك و`obfuscatedProfileId` لمعرّف المتجر. قبل فتح نافذة الدفع:

- يفحص التطبيق `billingResult.responseCode` الحقيقي بالإضافة إلى الحقل القديم الذي تضبطه مكتبة Google إلى `OK`؛ فشل الاستعلام لا يُعامل كقائمة مشتريات فارغة.
- يختار توكن الاستبدال المربوط بالحساب والمتجر المفتوح فقط، ويتجاهل التوكنات المربوطة صراحةً بمتاجر أخرى عند تغيير اشتراك موجود.
- يرفض أي توكن legacy بلا `obfuscatedProfileId` في مسار الشراء أو التغيير؛ لا يُستخدم كـ`oldPurchase` حتى لو كان للمتجر استحقاق خادمي.
- إذا وجد Play شراءً دقيقاً للمتجر لم يتصالح معه الخادم بعد، لا يبدأ شراءاً ثانياً ويطلب استعادة المشتريات أولاً.
- الترقية إلى باقة أعلى في الدورة نفسها تستخدم السعر النسبي، والتغيير إلى دورة أخرى يبدأ بالسعر الكامل، والتحويل السنوي إلى الشهري داخل الباقة نفسها بلا احتساب نسبي. أي منتج أدنى يرمي `GOOGLE_SUBSCRIPTION_DOWNGRADE_NOT_ALLOWED` قبل إطلاق `BillingFlow`، ولا يستخدم `ReplacementMode.deferred` لإنشاء خفض جديد.

الاستعادة مختلفة عمداً عن الشراء: تستقبل `storeId` وتختار مرشحاً واحداً مربوطاً بالمتجر المفتوح، وتتجاهل المتاجر الأخرى. عند غياب التطابق يمكنها إرسال مرشح legacy واحد فقط إلى الخادم مع `recoveryRequested=true`؛ التعدد أو السلسلة المبهمة يفشلان بأمان. إذا أعاد Google شراءً قائماً يحمل `pendingPurchaseUpdate` من خفض مؤجل أُنشئ سابقاً، تعالج الاستعادة المنتج والتوكن الحاليين كاستحقاق قابل للتحقق، بينما يبقى الحدث نفسه `pending` خارج الاستعادة ولا يُسمح ببدء خفض جديد. Build `23` account-only وlinked predecessor/out-of-app لا ينتقلان بعد حذف الحساب إلا عبر RPC ذرية تثبت حذف الهوية القديمة وغياب أي ربط أو entitlement منافس. شراء Google خارج التطبيق يُكتشف عند الفتح البارد أو الاستئناف ضمن مصالحة محدودة مرة كل 15 دقيقة.

## التحقق الخادمي والإنتاج

مشروع Supabase الصحيح هو `exxayzlklvgeyqhvtzgi`:

- migrations مطبقة حتى `20260901012000`. تضيف migration الأخيرة استعادة Google orphan/lineage الذرية بمعامل افتراضيه `false` وأقفال token hashes مرتبة، مع بقاء مسار المتجر بلا تجربة من Build `25`.
- `verify-store-purchase` v28 `ACTIVE` مع `verify_jwt=false` والتحقق اليدوي من الجلسة وSHA-256 للحزمة `ecee6ad20c0efd25c452d581552aafe903be8316cc43c767a0b551bd83dd7262`، و`refresh-store-entitlements` v7 نشطة. استجاب runtime الحي لطلب بلا جلسة بـ`401`.
- `dispatch-webhooks` v11، `damanak-api` v10، `analyze-claim-ai` v10، `import-products-ai` v12، `legal` v16، و`warranty-card` v14 نشطة.
- جدولة `damanak-entitlement-refresh` تعمل كل 5 دقائق.
- يربط الخادم الإيصال بمتجر واحد ذرياً، ويرفض فرع Google الشقيق أو replay لتوكن لم يعد current receipt.
- يقر Google purchase بعد تطبيق الاستحقاق في مهمة خادمية غير حاجبة، وتعيد مهمة التحديث المحاولة عند الحاجة. Build `26` ينفذ إقرار الجهاز أيضاً ويفحص `BillingResultWrapper.responseCode`؛ non-OK يبقي المعاملة قابلة لإعادة المحاولة. حالة الإقرار غير المعروفة تفشل مغلقة.
- Apple orphan يستخدم `@apple/app-store-server-library@3.1.0` والجذور الرسمية الثلاث المثبتة؛ طابقت بصمات DER الملفات المنشورة من Apple. يبدأ التحقق بالفاحص المتصل، ثم يعيد المهلة والحالات `VERIFICATION_FAILURE` و`RETRYABLE_VERIFICATION_FAILURE` و`INVALID_CERTIFICATE` عبر الفاحص الرسمي عند `signedDate`. فقط إذا أعاد الفاحصان `VERIFICATION_FAILURE=1` يعمل فاحص توافق محلي صارم. v28 لا تعتمد على Node `X509Certificate` غير المنفذة في Deno `2.1.4`؛ تستخدم محلل DER الصارم وتحقق `jsrsasign@11.1.5` للسلسلة وJWS، وتفرض ES256/P-256 وBasicConstraints وأسماء DER والتواقيع وOIDي Apple وصلاحية الشهادات والجذر المثبت، وترفض SHA-1 للـleaf والـintermediate، ولا تثق بـ`x5c[2]`. تبقى `FAILURE=7` ومعرّف التطبيق والبيئة وطول السلسلة مرفوضة. حتى بعد نجاح التوقيع يجب أن تتطابق الحقول الستة مع App Store Server API وأن تنجح حواجز الحساب المحذوف والربط الذري.
- دليل قبول تاريخي قبل reset: أعادت `verify-store-purchase` HTTP `200` عند `2026-09-01 09:22:26` بتوقيت الرياض، وصار الاشتراك حينها `scale/active/source=store/provider=app_store/monthly/sandbox` مع `auto_renews=true` وentitlement غير superseded ونهاية فترة `2026-09-02 09:22:26.976016+03:00`. أعاد `refresh-store-entitlements` HTTP `200` وحدّث التحقق عند `09:25:03`. بعد reset المطلوب من المستخدم، الحالة الحية الحالية هي صفر `source=store` وصفر entitlement حالي وصفر refresh candidate؛ بقي tombstone تاريخي `revoked` فقط، وأكد فحص بعد دورة الجدولة عدم إعادة تفعيله.
- `supabase/schema.sql` حجمه `395,324` بايت وSHA-256 هو `5A1E083994801006E3C620E6D91C611E2097D3B216FA1D3E8123F97DAD2F8F3C`. اختبار PGlite يطبق migration ويثبت تطابق تعريف RPC بين المخطط والترحيل.

نجحت اختبارات SQL الحية التالية داخل `BEGIN/ROLLBACK`:

- `supabase/tests/orphan_store_lineage_recovery_live.sql`
- `supabase/tests/subscription_lifecycle_live.sql`
- `supabase/tests/account_deletion_successor_live.sql`
- `supabase/tests/warranty_tenant_integrity_live.sql`
- `supabase/tests/store_creation_without_repeat_trial_live.sql`

لا تضع مفاتيح Apple أو حساب خدمة Google في التطبيق أو Git. الأسرار تبقى في Supabase/GitHub فقط، ولا تُنقل migrations ضمانك إلى مشروع آخر.

## ما لا يزال خارج الكود

- لا يوجد مستهلك Google RTDN/Pub/Sub حالياً. شراء خارج التطبيق يُصالح عند فتح التطبيق؛ إذا لم يفتحه المستخدم ضمن مهلة إقرار Google فلا يمكن اكتشاف التوكن الجديد دورياً من دون RTDN.
- إشعارات Apple Server Notifications V2 ليست مهيأة. التحديث المجدول يغطي الإيصالات المعروفة، لكنه لا يحل محل اختبار الإشعارات الفورية.
- legacy Google غير المربوط ممنوع عمداً من تغيير الخطة. قد تنجح استعادته إذا أثبت الخادم سلسلة واحدة دقيقة؛ عند الغموض يجب إدارة الاشتراك من Google Play أو استخدام الحساب الصحيح.
- حذف حساب ضمانك لا يلغي اشتراك المتجر تلقائياً؛ واجهة الحذف تحذر المستخدم وتوجه الاشتراك المعروف إلى إدارة المتجر. الاستعادة الآمنة لا تستبدل قرار الإلغاء.

## اختبار القبول الإلزامي لـBuild 29 بعد رفع TestFlight

Build `29` هو مرشح قبول iPhone للبنية الجديدة بعد رفعه إلى TestFlight، من دون تغيير App Review الجاري. Build `28` يبقى أحدث بناء `VALID` تاريخياً حتى اكتمال ذلك. Android Build `29` غير مرفوع؛ عند قرار إصداره استخدم مسار Play الاختباري ولا تعتمد على APK الجانبي وحده لقبول فوترة المتجر:

1. شراء شهري وسنوي على iPhone وAndroid والتأكد أن السعر والعملة من المتجر وأن الاستحقاق لا يفتح قبل تحقق الخادم.
2. استعادة على الجهاز نفسه وجهاز آخر، مع متجر واحد ثم حساب ضمانك يملك متجرين.
3. دفعة Google معلّقة، ثم إكمالها وإلغاؤها، والتأكد من عدم فتح تغيير ثانٍ.
4. ترقية وتبديل دورة الفوترة، ثم محاولة اختيار كل باقة أدنى والتأكد أن الزر معطل ولا تفتح نافذة المتجر.
5. إلغاء التجديد، ومشكلة الدفع، والسماح المؤقت، والانتهاء، والاسترداد أو السحب.
6. شراء Google خارج التطبيق ثم فتح ضمانك، واختبار المصالحة الباردة والاستئناف.
7. محاولة إيصال حساب أو متجر آخر، وتوكن legacy مبهم، وتعدد مشتريات، والتأكد من الرفض من دون فتح الدفع.
8. خمس دورات خلفية/استئناف، وفتح بارد، واتصال متقطع، وVoiceOver/TalkBack على جهازين حقيقيين.
9. حساب وجهاز سبق لهما استهلاك التجربة: إنشاء المتجر ينجح، وتظهر حصة صفر، ولا يمكن إصدار ضمان أو إضافة منتج أو دعوة أو مطالبة قبل التفعيل.
10. **ناجح تاريخياً على Build 26 في الحالة المبلغ عنها فقط:** اشتراك Sandbox على حساب ضمانك محذوف، ثم حساب/متجر نهائي واحد، وضغطة «استعادة المشتريات» واحدة بعد Edge v28؛ أعاد التحقق HTTP `200`. أُعيد ضبط الاستحقاق المحلي لاحقاً بطلب المستخدم، لذلك لم يعد فعالاً الآن.
11. قبل اختبار شراء Apple جديد من الصفر، امسح Purchase History لحساب `sandbox` من جهة Apple؛ تنظيف Supabase وحده لا يمحو ملكية المتجر وقد تجعل الاستعادة تعيد السجل القديم.

لا تُسجل أي بند بوصفه ناجحاً قبل تنفيذه فعلياً وتوثيق الجهاز والحساب والبيئة والنتيجة.
