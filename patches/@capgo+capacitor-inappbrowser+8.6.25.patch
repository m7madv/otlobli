diff --git a/node_modules/@capgo/capacitor-inappbrowser/android/build.gradle b/node_modules/@capgo/capacitor-inappbrowser/android/build.gradle
index 9979670..fcfd30a 100644
--- a/node_modules/@capgo/capacitor-inappbrowser/android/build.gradle
+++ b/node_modules/@capgo/capacitor-inappbrowser/android/build.gradle
@@ -38,8 +38,8 @@ android {
         abortOnError = false
     }
     compileOptions {
-        sourceCompatibility JavaVersion.VERSION_21
-        targetCompatibility JavaVersion.VERSION_21
+        sourceCompatibility JavaVersion.VERSION_17
+        targetCompatibility JavaVersion.VERSION_17
     }
 }
 
diff --git a/node_modules/@capgo/capacitor-inappbrowser/android/src/main/java/ee/forgr/capacitor_inappbrowser/CapgoInAppBrowserPlugin.java b/node_modules/@capgo/capacitor-inappbrowser/android/src/main/java/ee/forgr/capacitor_inappbrowser/CapgoInAppBrowserPlugin.java
index ae420ed..16d7831 100644
--- a/node_modules/@capgo/capacitor-inappbrowser/android/src/main/java/ee/forgr/capacitor_inappbrowser/CapgoInAppBrowserPlugin.java
+++ b/node_modules/@capgo/capacitor-inappbrowser/android/src/main/java/ee/forgr/capacitor_inappbrowser/CapgoInAppBrowserPlugin.java
@@ -18,6 +18,7 @@ import android.text.TextUtils;
 import android.util.Log;
 import android.webkit.CookieManager;
 import android.webkit.PermissionRequest;
+import android.webkit.ValueCallback;
 import android.webkit.WebView;
 import androidx.activity.result.ActivityResult;
 import androidx.activity.result.ActivityResultLauncher;
@@ -787,10 +788,24 @@ public class CapgoInAppBrowserPlugin extends Plugin implements WebViewDialog.Per
 
     @PluginMethod
     public void clearAllCookies(PluginCall call) {
-        CookieManager cookieManager = CookieManager.getInstance();
-        cookieManager.removeAllCookies(null);
-        cookieManager.flush();
-        call.resolve();
+        // otlobli: removeAllCookies(null) is asynchronous - the previous code
+        // called call.resolve() immediately after issuing it, without waiting
+        // for the actual removal to finish. Callers that reopen a webview
+        // right after this promise resolves (e.g. switching stores) could
+        // then race a brand-new page load against cookies that hadn't
+        // actually been cleared yet on a busy/slow device, making the
+        // cleanup silently ineffective sometimes. Wait for the real
+        // onReceiveValue callback before resolving.
+        this.getActivity().runOnUiThread(() -> {
+            CookieManager cookieManager = CookieManager.getInstance();
+            cookieManager.removeAllCookies(new ValueCallback<Boolean>() {
+                @Override
+                public void onReceiveValue(Boolean value) {
+                    cookieManager.flush();
+                    call.resolve();
+                }
+            });
+        });
     }
 
     @PluginMethod
diff --git a/node_modules/@capgo/capacitor-inappbrowser/android/src/main/java/ee/forgr/capacitor_inappbrowser/WebViewDialog.java b/node_modules/@capgo/capacitor-inappbrowser/android/src/main/java/ee/forgr/capacitor_inappbrowser/WebViewDialog.java
index 8ae3e61..12fb994 100644
--- a/node_modules/@capgo/capacitor-inappbrowser/android/src/main/java/ee/forgr/capacitor_inappbrowser/WebViewDialog.java
+++ b/node_modules/@capgo/capacitor-inappbrowser/android/src/main/java/ee/forgr/capacitor_inappbrowser/WebViewDialog.java
@@ -215,6 +215,35 @@ public class WebViewDialog extends Dialog implements ProxyResponseRouting.ProxyR
     private static final int REQUEST_CONNECT_TIMEOUT_MS = 15_000;
     private static final int REQUEST_READ_TIMEOUT_MS = 30_000;
     private static final int MAX_WEBVIEW_PROXY_REDIRECTS = 10;
+
+    // otlobli: every native proxy fetch below is routed through a Cloudflare
+    // Worker relay instead of opening a connection to requestContext.url
+    // directly. The relay forwards the request byte-for-byte and streams the
+    // response back unmodified - it exists only so the *device's* IP
+    // (geo-blocked in Syria) isn't what shein.com sees; Cloudflare's network
+    // already reaches it fine (same relay used by the product scraper Worker
+    // this file lives alongside). See worker/src/index.js `handleRelay` and
+    // CHAT_SUMMARY.md section 9 for the full picture.
+    // otlobli: sharded across 3 identical Workers (same code, same secret) -
+    // a real product/feed page fires dozens of requests "simultaneously",
+    // but HttpURLConnection (like most HTTP stacks) caps concurrent
+    // connections *per host*, so funneling every one of them through a
+    // single relay hostname serialized them into a trickle (observed: a
+    // heavy page stuck on its loading skeleton for a minute+). Spreading
+    // requests across 3 hostnames gives roughly 3x the effective concurrency.
+    private static final String[] OTLOBLI_RELAY_HOSTS = {
+        "talabieh-shein.talabieh.workers.dev",
+        "talabieh-shein-b.talabieh.workers.dev",
+        "talabieh-shein-c.talabieh.workers.dev",
+    };
+    private static String otlobliPickRelayUrl() {
+        String host = OTLOBLI_RELAY_HOSTS[java.util.concurrent.ThreadLocalRandom.current().nextInt(OTLOBLI_RELAY_HOSTS.length)];
+        return "https://" + host + "/relay";
+    }
+    // Substituted at build time from the OTLOBLI_RELAY_KEY env var by
+    // scripts/inject-relay-key.cjs (see package.json postinstall) - never
+    // committed in real form, see CHAT_SUMMARY.md section 9.
+    private static final String OTLOBLI_RELAY_KEY = "OTLOBLI_RELAY_KEY_PLACEHOLDER";
     private static final int MAX_MANAGED_DOWNLOAD_REDIRECTS = 10;
     private static final int BLOB_DOWNLOAD_CHUNK_BYTES = 64 * 1024;
     private static final long MAX_LEGACY_BLOB_DOWNLOAD_BYTES = 512L * 1024L;
@@ -5586,17 +5615,50 @@ public class WebViewDialog extends Dialog implements ProxyResponseRouting.ProxyR
         );
     }
 
+    private static final int OTLOBLI_MAX_TRANSIENT_RETRIES = 2;
+
     private NativeResponseData performNativeRequest(NativeRequestContext requestContext) throws IOException {
         URL url = new URL(requestContext.url);
         if (!ProxyRequestSupport.supportsNativeHttpRequest(url)) {
             throw new IOException("Unsupported proxy URL: " + requestContext.url);
         }
-        HttpURLConnection conn = (HttpURLConnection) url.openConnection();
+        // otlobli: a cold app launch right after the device's network changed
+        // (e.g. a VPN just got toggled off) can hit the relay before the
+        // network stack has settled - DNS/route failures that clear up
+        // within a second on their own (confirmed: net::ERR_NAME_NOT_RESOLVED
+        // followed immediately by a clean load on manual retry). Retry
+        // before surfacing the failure to the WebView.
+        IOException lastTransientError = null;
+        for (int attempt = 0; attempt <= OTLOBLI_MAX_TRANSIENT_RETRIES; attempt++) {
+            try {
+                return performNativeRequestOnce(requestContext);
+            } catch (java.net.UnknownHostException | java.net.ConnectException | java.net.SocketTimeoutException e) {
+                lastTransientError = e;
+                if (attempt == OTLOBLI_MAX_TRANSIENT_RETRIES) break;
+                try {
+                    Thread.sleep(400L * (attempt + 1));
+                } catch (InterruptedException interrupted) {
+                    Thread.currentThread().interrupt();
+                    break;
+                }
+            }
+        }
+        throw lastTransientError;
+    }
+
+    private NativeResponseData performNativeRequestOnce(NativeRequestContext requestContext) throws IOException {
+        // otlobli: open the connection to one of the relay shards (see
+        // OTLOBLI_RELAY_HOSTS doc comment above), not to `requestContext.url`
+        // directly - the real target travels in the X-Relay-Url header
+        // instead of being the connection's actual URL.
+        HttpURLConnection conn = (HttpURLConnection) new URL(otlobliPickRelayUrl()).openConnection();
         try {
             conn.setConnectTimeout(REQUEST_CONNECT_TIMEOUT_MS);
             conn.setReadTimeout(REQUEST_READ_TIMEOUT_MS);
             conn.setRequestMethod(requestContext.method != null ? requestContext.method : "GET");
             conn.setInstanceFollowRedirects(false);
+            conn.setRequestProperty("X-Relay-Key", OTLOBLI_RELAY_KEY);
+            conn.setRequestProperty("X-Relay-Url", requestContext.url);
 
             // Strip conditional cache validators on the wire so upstream cannot return 304/3xx,
             // which WebResourceResponse.setStatusCodeAndReasonPhrase rejects (kills the renderer).
diff --git a/node_modules/@capgo/capacitor-inappbrowser/dist/esm/definitions.d.ts b/node_modules/@capgo/capacitor-inappbrowser/dist/esm/definitions.d.ts
index 19497b5..2a53c1f 100644
--- a/node_modules/@capgo/capacitor-inappbrowser/dist/esm/definitions.d.ts
+++ b/node_modules/@capgo/capacitor-inappbrowser/dist/esm/definitions.d.ts
@@ -673,6 +673,18 @@ export interface OpenWebViewOptions {
      * Test URL: https://capgo.app
      */
     disableGoBackOnNativeApplication?: boolean;
+    /**
+     * otlobli patch: if true, the page calling window.close() on itself does
+     * NOT dismiss the native browser (iOS only - Android has no such
+     * override). Upstream unconditionally wires window.close() straight to
+     * a native dismiss with no gating; some third-party anti-bot scripts
+     * (observed: Cloudflare's challenge page) call window.close()
+     * defensively when they detect an atypical/embedded browser context,
+     * which otherwise closes this entire in-app browser instantly with no
+     * app-visible error.
+     * @default false
+     */
+    ignorePageWindowClose?: boolean;
     /**
      * Open url in a new window fullscreen
      * isPresentAfterPageLoad: if true, the browser will be presented after the page is loaded, if false, the browser will be presented immediately.
diff --git a/node_modules/@capgo/capacitor-inappbrowser/ios/Sources/InAppBrowserPlugin/InAppBrowserPlugin.swift b/node_modules/@capgo/capacitor-inappbrowser/ios/Sources/InAppBrowserPlugin/InAppBrowserPlugin.swift
index 116e296..e35f8ac 100644
--- a/node_modules/@capgo/capacitor-inappbrowser/ios/Sources/InAppBrowserPlugin/InAppBrowserPlugin.swift
+++ b/node_modules/@capgo/capacitor-inappbrowser/ios/Sources/InAppBrowserPlugin/InAppBrowserPlugin.swift
@@ -844,6 +844,7 @@ public class CapgoInAppBrowserPlugin: CAPPlugin, CAPBridgedPlugin {
         let ignoreUntrustedSSLError = call.getBool("ignoreUntrustedSSLError", false)
         let enableGooglePaySupport = call.getBool("enableGooglePaySupport", false)
         let activeNativeNavigationForWebview = call.getBool("activeNativeNavigationForWebview", true)
+        let ignorePageWindowClose = call.getBool("ignorePageWindowClose", false)
 
         self.isPresentAfterPageLoad = call.getBool("isPresentAfterPageLoad", false)
         let showReloadButton = call.getBool("showReloadButton", false)
@@ -965,6 +966,7 @@ public class CapgoInAppBrowserPlugin: CAPPlugin, CAPBridgedPlugin {
 
             // Set native navigation gestures before view loads
             webViewController.activeNativeNavigationForWebview = activeNativeNavigationForWebview
+            webViewController.ignorePageWindowClose = ignorePageWindowClose
 
             // Update the webview's gesture property (if webview already exists)
             webViewController.updateNavigationGestures()
diff --git a/node_modules/@capgo/capacitor-inappbrowser/ios/Sources/InAppBrowserPlugin/ProxySchemeHandler.swift b/node_modules/@capgo/capacitor-inappbrowser/ios/Sources/InAppBrowserPlugin/ProxySchemeHandler.swift
index 1c70884..be2b8ac 100644
--- a/node_modules/@capgo/capacitor-inappbrowser/ios/Sources/InAppBrowserPlugin/ProxySchemeHandler.swift
+++ b/node_modules/@capgo/capacitor-inappbrowser/ios/Sources/InAppBrowserPlugin/ProxySchemeHandler.swift
@@ -449,6 +449,34 @@ public class ProxySchemeHandler: NSObject, WKURLSchemeHandler, URLSessionTaskDel
     /// still reach finish() or the scheme task strands.
     static let redirectStatusCodes: Set<Int> = [301, 302, 303, 307, 308]
 
+    /// otlobli: every native fetch this handler makes is routed through a
+    /// Cloudflare Worker relay instead of contacting the target host
+    /// directly. The relay forwards the request byte-for-byte and streams
+    /// the response back unmodified - it exists only so the *device's* IP
+    /// (geo-blocked in Syria) isn't what shein.com sees; Cloudflare's
+    /// network already reaches it fine (same relay used by the product
+    /// scraper Worker this file lives alongside). See worker/src/index.js
+    /// `handleRelay` and CHAT_SUMMARY.md section 9 for the full picture.
+    // otlobli: sharded across 3 identical Workers (same code, same secret) -
+    // a real product/feed page fires dozens of requests "simultaneously",
+    // but URLSession (like most HTTP stacks) caps concurrent connections
+    // *per host*, so funneling every one of them through a single relay
+    // hostname serialized them into a trickle (observed: a heavy page stuck
+    // on its loading skeleton for a minute+). Spreading requests across 3
+    // hostnames gives roughly 3x the effective concurrency.
+    static let otlobliRelayHosts = [
+        "talabieh-shein.talabieh.workers.dev",
+        "talabieh-shein-b.talabieh.workers.dev",
+        "talabieh-shein-c.talabieh.workers.dev",
+    ]
+    static var otlobliRelayURL: URL {
+        URL(string: "https://\(otlobliRelayHosts.randomElement()!)/relay")!
+    }
+    // Substituted at build time from the OTLOBLI_RELAY_KEY env var by
+    // scripts/inject-relay-key.cjs (see package.json postinstall) - never
+    // committed in real form, see CHAT_SUMMARY.md section 9.
+    static let otlobliRelayKey = "OTLOBLI_RELAY_KEY_PLACEHOLDER"
+
     weak var plugin: CapgoInAppBrowserPlugin?
     private var pendingTasks: [String: PendingProxyTask] = [:]
     private var stoppedRequests: [String: UUID] = [:]
@@ -646,6 +674,19 @@ public class ProxySchemeHandler: NSObject, WKURLSchemeHandler, URLSessionTaskDel
                     // So just stop processing and let the owning path complete it.
                     return
                 }
+                // otlobli: a delayed async retry here (scheduling a future
+                // executeNativePipeline call via asyncAfter) widened the
+                // window for a WKURLSchemeTask to receive a terminal call
+                // after WebKit already considers it done/replaced - exactly
+                // the "uncatchable NSException" hazard documented on
+                // completeSchemeTask below, and the prime suspect for a
+                // crashed WebContent process (reported symptom: the page
+                // freezes into what looks like a static snapshot - pinch-
+                // zoom, a UIKit-level gesture, still works; nothing else
+                // does). Removed - fail immediately instead, same as before
+                // retries were ever added. Android's retry is unaffected:
+                // it's a synchronous loop inside one blocking call, not a
+                // delayed closure that can outlive the task's lifecycle.
                 self.failSchemeTask(pendingTask, with: error)
                 return
             }
@@ -671,24 +712,40 @@ public class ProxySchemeHandler: NSObject, WKURLSchemeHandler, URLSessionTaskDel
                 return
             }
             let responseHeaders = ProxySchemeRequestSupport.normalizedResponseHeaders(from: httpResponse)
-            pendingTask.requestContext.url = ProxySchemeRequestSupport.resolvedResponseURL(
-                response,
-                fallback: pendingTask.requestContext.url
-            )
+            // otlobli: response.url is the relay endpoint, not the real target -
+            // pendingTask.requestContext.url already holds the real one (set when
+            // this fetch was dispatched), so leave it alone instead of overwriting
+            // it from the transport URL.
             let nativeResponse = NativeResponseData(
                 statusCode: httpResponse?.statusCode ?? 200,
                 headers: responseHeaders,
                 body: data ?? Data(),
                 contentType: responseHeaders["Content-Type"] ?? responseHeaders["content-type"] ?? "application/octet-stream"
             )
-            self.syncResponseCookies(
-                from: response,
-                fallbackURL: pendingTask.requestContext.url
-            ) {
+            let finishUp: () -> Void = {
                 guard let pendingTask = self.pendingTask(for: requestId) else { return }
                 pendingTask.responseData = nativeResponse
                 self.executeInboundDecision(requestId: requestId)
             }
+            // otlobli: Set-Cookie headers are relayed verbatim from shein.com, but
+            // the HTTP transaction itself happened against the relay's domain - the
+            // system's automatic cookie jar would reject them as cross-domain.
+            // Parse them manually against the real target URL instead.
+            //
+            // Deliver content immediately rather than gating finishUp() on the
+            // cookie write finishing first: WKHTTPCookieStore.setCookie is an
+            // async, cross-process call, and gating every single resource on
+            // a full page behind one (most pages set cookies on far more than
+            // just the main document) backed the main thread up badly enough
+            // to look like a frozen WebView - pinch-zoom (pure UIKit) still
+            // worked, nothing that needed the main run loop did. A few
+            // milliseconds' lag before a cookie is persisted is harmless here;
+            // nothing reads it synchronously.
+            let cookies = ProxySchemeRequestSupport.responseCookies(from: responseHeaders, fallback: pendingTask.requestContext.url)
+            finishUp()
+            if !cookies.isEmpty {
+                self.writeCookiesOnMain(cookies) {}
+            }
         }
 
         taskLock.lock()
@@ -819,23 +876,35 @@ public class ProxySchemeHandler: NSObject, WKURLSchemeHandler, URLSessionTaskDel
             return
         }
 
+        let responseHeaders = ProxySchemeRequestSupport.normalizedResponseHeaders(from: response)
         let redirectResponse = NativeResponseData(
             statusCode: response.statusCode,
-            headers: ProxySchemeRequestSupport.normalizedResponseHeaders(from: response),
+            headers: responseHeaders,
             body: Data(),
             contentType: response.value(forHTTPHeaderField: "Content-Type") ?? "application/octet-stream"
         )
 
-        pendingTask.requestContext.url = ProxySchemeRequestSupport.resolvedResponseURL(
-            response,
-            fallback: pendingTask.requestContext.url
-        )
+        // otlobli: this Set-Cookie (if any) belongs to the URL that issued the
+        // redirect, i.e. the pre-redirect target - capture it before overwriting
+        // requestContext.url below.
+        let preRedirectURL = pendingTask.requestContext.url
+        // response.url is the relay endpoint, not the real target. request.url is
+        // the one URLSession resolved from the relay's Location header, which the
+        // Worker normalizes to an absolute shein.com URL, so it's safe to use here.
+        pendingTask.requestContext.url = request.url?.absoluteString ?? preRedirectURL
         pendingTask.responseData = redirectResponse
         pendingTask.redirectRequest = request
         completionHandler(nil)
 
-        syncResponseCookies(from: response, fallbackURL: pendingTask.requestContext.url) { [weak self] in
-            self?.executeInboundDecision(requestId: requestId)
+        // otlobli: manual header parsing against preRedirectURL instead of
+        // syncResponseCookies(from: response, ...) - see the matching comment in
+        // executeNativePipeline's dataTask completion for why. Also don't gate
+        // executeInboundDecision on the cookie write finishing - same main-
+        // thread-backup reasoning as there.
+        let cookies = ProxySchemeRequestSupport.responseCookies(from: responseHeaders, fallback: preRedirectURL)
+        executeInboundDecision(requestId: requestId)
+        if !cookies.isEmpty {
+            writeCookiesOnMain(cookies) {}
         }
     }
 
@@ -958,10 +1027,13 @@ public class ProxySchemeHandler: NSObject, WKURLSchemeHandler, URLSessionTaskDel
     }
 
     private func makeURLRequest(from context: NativeRequestContext) throws -> URLRequest {
-        guard let url = URL(string: context.url) else {
+        guard URL(string: context.url) != nil else {
             throw ProxySchemeRequestSupport.RequestBuildError.invalidURL
         }
-        var request = URLRequest(url: url)
+        // otlobli: fetch via the relay (see otlobliRelayURL doc comment above)
+        // instead of `context.url` directly - the real target travels in the
+        // X-Relay-Url header instead of being the request's actual URL.
+        var request = URLRequest(url: ProxySchemeHandler.otlobliRelayURL)
         request.httpMethod = context.method
         // URLSession will auto-decompress gzip/br/zstd responses only when
         // Accept-Encoding is NOT present in the request headers (URLSession
@@ -971,9 +1043,11 @@ public class ProxySchemeHandler: NSObject, WKURLSchemeHandler, URLSessionTaskDel
         // caller will handle decompression and passes the raw compressed bytes
         // through, which the WKURLSchemeTask then renders as gibberish in the
         // WebView. Strip it so URLSession's transparent decompression kicks in.
-        let filteredHeaders = context.headers.filter { key, _ in
+        var filteredHeaders = context.headers.filter { key, _ in
             key.caseInsensitiveCompare("Accept-Encoding") != .orderedSame
         }
+        filteredHeaders["X-Relay-Key"] = ProxySchemeHandler.otlobliRelayKey
+        filteredHeaders["X-Relay-Url"] = context.url
         request.allHTTPHeaderFields = filteredHeaders
         if
             ProxySchemeRequestSupport.supportsRequestBody(method: context.method),
diff --git a/node_modules/@capgo/capacitor-inappbrowser/ios/Sources/InAppBrowserPlugin/WKWebViewController.swift b/node_modules/@capgo/capacitor-inappbrowser/ios/Sources/InAppBrowserPlugin/WKWebViewController.swift
index 97a2fc1..7cd9d05 100644
--- a/node_modules/@capgo/capacitor-inappbrowser/ios/Sources/InAppBrowserPlugin/WKWebViewController.swift
+++ b/node_modules/@capgo/capacitor-inappbrowser/ios/Sources/InAppBrowserPlugin/WKWebViewController.swift
@@ -353,6 +353,23 @@ open class WKWebViewController: UIViewController, WKScriptMessageHandler {
     var shareDisclaimer: [String: Any]?
     var shareSubject: String?
     var didpageInit = false
+    // otlobli: the plugin's own bridge script (below) unconditionally
+    // overrides window.close() on the page and wires it straight to a
+    // native dismiss with zero gating - any script on the page (not just
+    // ours) calling window.close() tears the whole browser down instantly.
+    // Confirmed real-world trigger: Cloudflare's bot-mitigation challenge
+    // ("Just a moment...") commonly calls window.close() defensively once
+    // its environment fingerprinting flags an embedded WKWebView as an
+    // atypical/automated context - which happens to be exactly this app's
+    // situation (SHEIN reached via VPN, inside a bare WKWebView with no real
+    // tab history). That looked identical to "SHEIN opens then kicks the
+    // user out to the app's home screen within ~2 seconds" - never touching
+    // pageLoadError or any of the app's own JS-side error handling, since
+    // this path is entirely native and unconditional. This app never opens
+    // pages that legitimately need window.close() to work (it always opens
+    // a fresh top-level URL via openWebView, never via window.open() from
+    // another page), so ignoring it here is safe.
+    open var ignorePageWindowClose = false
     open var closeModal = false
     open var closeModalTitle = ""
     open var closeModalDescription = ""
@@ -1170,7 +1187,9 @@ open class WKWebViewController: UIViewController, WKScriptMessageHandler {
             print("[InAppBrowser - preShowScriptError]: Error!!!!")
             semaphore.signal()
         } else if message.name == "close" {
-            closeView()
+            if !ignorePageWindowClose {
+                closeView()
+            }
         } else if message.name == "hide" {
             guard allowWebViewJsVisibilityControl else {
                 return
@@ -2536,7 +2555,7 @@ extension WKWebViewController: WKUIDelegate {
     }
 
     public func webViewDidClose(_ webView: WKWebView) {
-        if webView == self.webView {
+        if webView == self.webView && !ignorePageWindowClose {
             closeView()
         }
     }
@@ -2756,6 +2775,14 @@ extension WKWebViewController: WKNavigationDelegate {
         emit("pageLoadError")
     }
 
+    // otlobli: when iOS kills the WebContent process under memory pressure
+    // (heavy SHEIN pages), the in-app browser goes black and stays black,
+    // forcing the user to quit and relaunch the whole app. Reload the webview
+    // automatically so it recovers itself instead.
+    public func webViewWebContentProcessDidTerminate(_ webView: WKWebView) {
+        webView.reload()
+    }
+
     public func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
         if let credentials = credentials,
            challenge.protectionSpace.receivesCredentialSecurely,
