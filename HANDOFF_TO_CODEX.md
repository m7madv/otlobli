# تسليم إلى Codex — الحالة الحالية v86.174 (2026-08-13)

> **هذا القسم هو الحالة الحالية.** أقسام v86.173 وما قبلها سجل سابق؛ المصدر الحالي هو الملفات العاملة غير الملتزمة.

## ابدأ من هنا

- الشجرة العاملة الوحيدة المقصودة: `C:\Users\MOHAMMAD\Projects\SHEIN IN SIRYA\.claude\worktrees\cart-product-white-screen-33b3bf`
- الفرع: `claude/temu-issues-v86134`
- الملفات العاملة أحدث بكثير من آخر commit `7a9117c`، وكل الدفعة ما زالت **غير ملتزم بها وغير مدفوعة**. لا تستخدم `git log` كمصدر حالة، ولا تعمل reset/checkout أو تستبدلها من فرع أقدم.
- اقرأ بالترتيب: `AGENTS.md`، ثم أعلى `CURRENT_STATE.md`، ثم أعلى `AI-HANDOFF.md`، ثم أعلى `docs/KNOWN_ISSUES_AND_DECISIONS.md`.
- النسخة الحالية: `86.174-personal-surface-colors/1036`.
- APK جهاز ARM64: `output/otlobli-v86.174-temu-personal-arm64-debug.apk`، الحجم `205,045,396` بايت، SHA-256 `408B41F463CB558BD3E75D2CF224D276AB9EFEF2EE3486D5AC1833783B3917EB`.
- APK المحاكي المثبت: `output/otlobli-v86.174-temu-personal-x86_64-debug.apk`، الحجم `224,468,290` بايت، SHA-256 `597DC0F2B741EB7ED5F640BE5877C7CE53669A50736F7292C231419A3C05981F`.
- النسخة مثبتة ومقاسة على محاكي Android 15 فقط. Note 8 غير متصل ولم تُثبت v86.174 عليه. iOS متزامن عند `86.174/1036`، بلا بناء Xcode أو قبول iPhone.

## ما تم في v86.174

1. **السبب المثبت:** لا يوجد dim عالق، وSurfaceFlinger أعاد sRGB و`dimmingRatio=1.0`، وأبيض Temu الحقيقي بقي `255`. طبقة Temu كانت تنتهي عند `y=2164` وشريط React يبدأ عند `y=2102`، أي تداخل `62px`، ثم يلقي `elevation=24dp` ظلاً رمادياً مقاساً فوق الشريط (`204 → 212 → 233 → 249`).
2. **الإصلاح المحدود:** أزيل elevation من طبقة Temu، وأصبح حجزها السفلي `90dp + navigationBars.bottom`. في APK المثبت تنتهي الطبقة عند `y=2101` ويبدأ الشريط عند `y=2102`، وخلفيته المقاسة `255,255,255` من دون تغيير أي CSS.
3. **حارس انحدار:** `npm run verify:store-surface` يمنع رجوع الظل/التداخل/dim ويحمي استعادة alpha وحفظ الجلسة ومسار لقطة SurfaceView.
4. **الانحدار:** حسابي → Temu حفظ جلسة Gecko، والخلفية/العودة حفظت الصفحة، ومنتج Temu فعلي فتح مع `otlobli-add-btn` ورجع طبيعياً، وبلاغ الهز عرض لقطة Temu غير سوداء ثم ترك نافذة تطبيق واحدة وخلفية شريط بيضاء بعد الإلغاء. لا FATAL أو ANR.

## ما يجب عمله تالياً

- ثبّت ARM64 v86.174 على Note 8 الحقيقي وكرر: بارد، Temu، منتج، حسابي/السلة والعودة، بلاغ الهز/إلغاء، خلفية/عودة، ورجوع. لا تدّع القبول قبل ذلك.
- بعد قبول اللون على الجهاز الحقيقي، ابدأ نسخة محدودة من `StoreSurfaceController` اعتماداً على حارس `verify:store-surface` الموجود، من دون refactor شامل أو تغيير الدفع/المحفظة/الطلبات.
- حافظ على كل ثوابت v86.173: قائمة المقاس البنيوية، `الحجم` و`.specTypes-*`، التقاط SurfaceView، والخلفية/لوحة البلاغات المنشورة.

## التحقق الحالي

- نجح `npm run build` و`npm run build:temu-personal`.
- نجحت `verify:shein-freeze-guard` و`verify:temu-size-gate` و`verify:store-surface` و`verify:performance-budget`.
- تمت مزامنة Android وiOS، وبُني Android القياسي ونسختا الشخصي x86_64/ARM64.
- Admin والخلفية لم يتغيرا، لذلك لم يُعاد نشرهما في v86.174.

---

# السجل السابق — جلسة 2026-08-12 (من v86.167 إلى v86.172)

هذا الملف مكتوب ليُقرأ قبل أي تعديل. كل بند مُصنَّف صراحةً إلى **مُثبَت بقياس على الجهاز** أو **غير مُثبَت**. لا تعامل غير المُثبَت كحقيقة، ولا تُعِد تجربة ما سقط.

---

## 1. أين كان الشغل في نهاية v86.172 (تاريخي)

- **الشجرة العاملة:** `.claude/worktrees/cart-product-white-screen-33b3bf`
- **الفرع:** `claude/temu-issues-v86134` — وآخر commit عليه هو `v86.134`
- **⚠️ كل شغل هذه الجلسة غير مُلتزَم (uncommitted).** لا يوجد commit ولا push. `git log` سيكذب عليك: أعلى نسخة مُلتزَمة في المستودع كله هي `v86.134`، بينما الملفات العاملة على `v86.172`.
- المستودع الرئيسي `C:\Users\MOHAMMAD\Projects\SHEIN IN SIRYA` على `claude/ios6-cover-fix` عند `v86.43` — متأخر ~130 نسخة. لا تعمل منه.
- **المُثبَّت على نوت 8:** `86.172-personal-invisible-gate-fix/1034`، الحزمة `output/otlobli-v86.172-temu-personal-arm64-debug.apk`، SHA-256 `91695CAA88B1DF2C4EBDB16FD9DAFBAB462F8BC4EA4950DF2D8E4B1E81080747`.

## 2. طلب مفتوح لم يُنفَّذ

**المستخدم طلب نسخة iPhone ولم تُبنَ.** السبب: بناء iOS يمر عبر `gh workflow run ios-unsigned-build.yml`، وهو يقرأ من GitHub، وكل الشغل غير مدفوع. تنفيذها يتطلب commit + push أولاً. لم يُتخذ هذا القرار.

ملاحظة على النطاق: إصلاحات هذه الجلسة تخص مسارين. `v86.168` و`v86.169` و`v86.170` و`v86.171` تنطبق على iOS أيضاً. أما `v86.172` فيخص `TEMU_PERSONAL_SITE_MODE` وهو **أندرويد فقط** ولا أثر له على iPhone. ولا يوجد أي قبول جهاز iPhone منذ `v86.160`.

---

## 3. ما أُصلح — ولماذا (كلها مُثبَتة)

### أ) بوابة «شغّل VPN» الكاذبة على منتجات السلة — v86.168

**الدليل القاطع:** بمراقبة `window.fetch` عبر CDP، لم ينطلق **ولا فحص جغرافي واحد** خلال المسار المعطوب كاملاً (منتج سلة Temu ← خروج ← السلة ← تبويب SHEIN ← ضغط المنتج)، بينما كان التطبيق يعرض «شغّل VPN». والخروج الفعلي **قطر (Vodafone Qatar)** — منطقة مدعومة — على جهاز بلا أي واجهة VPN. الخدمات الأربع نُفِّذت من داخل WebView التطبيق وأجابت `QA` خلال **52–77ms**.

سببان:
1. `openStoreProductFromCart()` كان يرفض اعتماداً على `vpnStateRef` مسجَّلة قبل دقائق، بلا أي فحص.
2. `switchSelectedStore()` كان يعيد `vpnStateRef` إلى `idle` ويرمي نتيجة خروج مدعومة مؤكَّدة. وصول المتجر خاصية للمتجر ويُعاد قياسه؛ **بلد الخروج خاصية لاتصال الجهاز ويجب أن يصمد عبر تبديل المتجر**. (`switchCartStore()` كانت أُصلحت لهذا سابقاً، أما تبديل المتجر فلا.)

**عرَض مضلِّل يستحق الانتباه:** الضغطة كانت تبدو **ميتة تماماً** لا رافضة، لأن الرسالة تختفي قبل أن تُرى. التقط الشاشة خلال ~1.3 ثانية من الضغط لتمسكها.

### ب) تراجع أدخلَته (أ) ثم أُصلح — v86.170

البديل الأول كان ينقل العميل إلى `home` **قبل** أن يجيب الفحص، فيهبط على بوابة تملأ الشاشة عند أي بطء أو فشل عابر. أبلغ المستخدم أن «السلة بطّلت تفتح». الآن: يبقى في السلة أثناء `checking`، وتأثير مربوط بـ`[vpnState, screen]` يدخل المتجر عند `ok` فقط، ويُفرغ الطابور ويذكر السبب داخل السلة عند فشل حقيقي.

**قاعدة مستخلصة:** لا تُصلح بوابة خاطئة بدفع العميل إلى مكان لم يختره.

### ج) الكتابة أثناء تحقق SHEIN البشري — v86.169

**تحقق محلول بشكل صحيح كان يعود بـ`Access timed out, please refresh the page and try again`.** السبب في كودنا، والقاعدة التي كُسرت موثَّقة في الكود نفسه: `ensureSheinSaudiStore()` تحمل التعليق «أثناء تحقق أنا إنسان: ممنوع أي إعادة تحميل/كتابة» وتخرج مبكراً. لكن المسارين اللذين **يدخلان** وضع التحقق كانا ينادِيان `writeSheinSaudiState()` أولاً (`otlobliEnterChallengeMode()` في `sheinHumanCheck.ts`، والفرع المبكر لرابط التحقق في `sheinBrowserScript.ts`). تلك الدالة تكتب **26 كوكي على `.shein.com`** + مفاتيح `localStorage`، فتتغيّر بصمة الجلسة بين إصدار الرمز والتحقق منه.

حُذفت الكتابتان، وصارت المنطقة تُكتب في فرع انتهاء التحقق بعد أن تُصفَّر `otlobliChallengeActive` وتعود الصفحة تفاعلية.

**غير مُتحقَّق:** لم يظهر تحقق حقيقي أثناء الاختبار. مُتحقَّق أن المنطقة لم تُفقد على الصفحات العادية (`country=SA`, `currency=USD`, `site_uid=pwar`, `language=ar`). **مشاهدة تحقق ينجح من أوله لآخره ما زالت مطلوبة.**

### د) ضغطة تبويب ميتة داخل Temu، ومنتج السلة يُفتح فوق السلة — v86.168

`openCart`/`openOrders`/`openProfile` كانت تخفي `InAppBrowser` (Chromium) ولا تخفي سطح Gecko الشخصي، فتتغيّر شاشة React تحت سطح ما زال مرسوماً — والضغطة تبدو ميتة. أُضيفت `hidePersonalTemuSurface()`.

وبطلب المستخدم صراحةً: منتج سلة Temu صار يُفتح فوق شاشة المتجر (`pendingBackTargetRef = 'home'` + تبديل الشاشة قبل `TemuEmbeddedBrowser.open()`) بدل أن يُفتح فوق السلة.

### هـ) البوابة غير المرئية التي «جمّدت» صفحة منتج Temu — v86.172 (الأهم)

**شكوى المستخدم:** منتج يُضاف بلا خيارات، ومحاولة الوصول للمقاس «تتسكّر».

**ما قِيس فعلياً:** صفحة المنتج لم يتغيّر فيها **بكسل واحد** خلال 4 ثوانٍ خمول، ومؤقتها التنازلي واقف. ومع ذلك:
- عبر تنقيح Gecko عن بُعد: `readyState` complete، 1097 عقدة، و`body.scrollTop = 600` طُبِّق فوراً و**تغيّر 87,671 بكسل على الشاشة**.
- مُستمِع بمستوى `window` وبـ`capture` سجّل **صفر** أحداث لمس من سحبة حقيقية.

رسم سليم + سكربت سليم + ولا لمسة تصل ⇒ **الحاجب فوق الصفحة لا فيها**. و`uiautomator dump` سمّاه: بوابة React «اتصالك من منطقة مدعومة، لكن منتجات تيمو لم تكتمل على هذا الجهاز» مع زر «إعادة تجهيز المتجر»، مفروشة فوق طبقة Temu. **الضغط على ذلك الزر غير المرئي بإحداثياته من الـdump نجح فوراً وتحرّكت الصفحة.**

**السبب:** Gecko يرسم فوق React، فالبوابة غير مرئية وتبتلع كل لمسة. والعميل لا يستطيع التمرير إلى الخيارات، فزر الإضافة لا يلتقط مقاساً — **العطلان عطل واحد**.

**الإصلاح:** البوابة تُخفي سطح المتجر عند ظهورها (تأثير `storeGateVisible`).

**غير مُتحقَّق:** لم يُستفَز ظهور البوابة عمداً بعد الإصلاح. مُتحقَّق بالآلية وبالسبب المُثبَت فقط. **استفزها (قطع الشبكة أثناء الجلسة) وتأكد أنها تظهر مرئية.**

---

## 4. ما جُرِّب وسقط — لا تُعِده

هذه ليست آراء؛ كلها سقطت بقياس على الجهاز:

1. **قفل تمرير عالق (`overflow:hidden`)** — كان الظن أن `showAddingOverlay()` تقفل التمرير و`removeOverlay()` المسار الوحيد لفكه. أُصلح فعلاً في **v86.171** (استعادة على `body` و`documentElement` + شفاء ذاتي في `tick()`) لأنه **عيب حقيقي بذاته**، لكنه **لم يكن سبب تجمّد صفحة Temu**؛ لم يتغيّر شيء بعده.
2. **انفصال العرض عن Gecko (compositor detach)** — جُرِّب `releaseSession()` + `setSession()` عند العودة من الإخفاء. **لم ينفع، ورُجِّع بالكامل.** لا تُعِده بلا دليل جديد.
3. **ضغط المعالج** — عملية المحتوى كانت على 66% وGPU على 58%، وبدا سبباً مقنعاً. **نُفي:** رئيسية Temu نفسها تعمل على **93%** وتُمرَّر وتستجيب بشكل طبيعي. الحمل العالي وحده لا يُثبت شيئاً هنا.

**الدرس المنهجي:** الصور وحدها أرسلت التشخيص في ثلاثة اتجاهات خاطئة. ما حسم الأمر هو قراءة الصفحة الحيّة وشجرة العرض الأصلية.

---

## 5. أداة جديدة متاحة الآن (استعملها)

نسخ **debug فقط** تُفعِّل تنقيح Gecko عن بُعد: `GeckoRuntimeSettings.remoteDebuggingEnabled(BuildConfig.DEBUG)` في `TemuEmbeddedBrowserPlugin`. نسخ الإصدار لا تفعّله أبداً.

```bash
adb forward tcp:9333 localabstract:com.otlobli.app/firefox-debugger-socket
```

البروتوكول JSON مسبوق بالطول (`<len>:<json>`): `listTabs` ← `getTarget` للحصول على `consoleActor` ← `evaluateJSAsync`. **انتظر رزمة `evaluationResult`؛ رزم `frameUpdate` تصل أولاً.** يوجد عميل جاهز صغير كُتب في الجلسة (نحو 40 سطر Node، بلا اعتماديات).

ومعها `adb shell uiautomator dump` — تُظهر الطبقات الأصلية وحدودها ونصوصها، وهي التي كشفت البوابة.

---

## 6. ما زال مفتوحاً

1. **بناء iPhone** — مطلوب من المستخدم ولم يُنفَّذ (انظر القسم 2).
2. **تحقق نهائي للبوابة غير المرئية** — استفزها وتأكد أنها تظهر مرئية.
3. **تحقق نهائي لإصلاح كوكيز التحقق** — راقب أول تحقق SHEIN حقيقي.
4. **لون الشريط السفلي داخل Temu يبدو باهتاً** — الفرق **مقيس**: الأخضر `(0,95,65)` داخل Temu مقابل `(0,105,72)` في التطبيق، مع أن الـCSS **متطابق حرفياً** (`#006948` في الاثنين، ولا وجود لـ`#005f41` في الكود كله). **السبب غير محسوم.** لا تفترض أنه `opacity`؛ الخلفية أيضاً تختلف (248 مقابل 255) والنسب غير متسقة مع طبقة موحدة أو gamma بسيط.
5. **«سلة تيمو بتفتح داخل السلة نفسها»** — شكوى من المستخدم **لم تُفهم بعد ولم يُعمل عليها**. اسأله عن المقصود قبل أي تعديل: هل تُفتح سلة Temu الحقيقية على الموقع بدل سلة otlobli، أم العكس؟

---

## 7. تحذيرات ثابتة

- **لا يوجد أي تجاوز لـCAPTCHA في أي من هذه التغييرات، ولا يجوز إدخاله.** SHEIN أصدر تحققاً بشرياً حقيقياً بعد الإصلاح وتركه التطبيق قابلاً للاستخدام خلف شريط إرشاده.
- كل التحقق في هذه الجلسة تم على **Galaxy Note 8 حقيقي** (`SM-N950F` / `988e16384e4f51395230`). **لا يوجد قبول جهاز iPhone.**
- الحارسان `verify:shein-freeze-guard` و`verify:temu-size-gate` وميزانية الأداء تمر جميعها. آخر قياسات: أكبر JS `1,083,079/1,200,000`، JS gzip `286,936/370,000`، CSS `69,766/70,000`، سكربتات المتجر `452,006/470,000`، مصدر SHEIN `549,957/600,000`.
