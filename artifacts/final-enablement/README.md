# Final-enablement artifacts

Generated validation reports and release-artifact inspection metadata belong
here. Signed binaries and credentials are never committed.

At documentation time there is no signed IPA/archive, AAB, or APK because the
required Apple and Android signing inputs are absent. The one permitted internal
candidate is the unsigned iOS GitHub artifact recorded in the final acceptance
report after its workflow completes. It is compile evidence only.
